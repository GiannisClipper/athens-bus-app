import { StyleSheet } from 'react-native';

const styles = StyleSheet.create( {

    infoMessage: {
        color: 'green',
        fontWeight: '600',
    },
    warningMessage: {
        color: 'orange',
        fontWeight: '600',
    },
    errorMessage: {
        color: 'tomato',
        fontWeight: '600',
    },
    workingIndicator: {
        color: 'orange',
    },

} );

export default styles;